# A sample Guardfile
# More info at https://github.com/guard/guard#readme

guard 'coffeescript',
  :input => 'src/coffee',
  :output => 'lib/js'

guard 'compass' do
  watch('^src/scss/(.*)\.s[ac]ss')
end
