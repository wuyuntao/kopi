kopi.module("kopi.db.adapters.base")
  .define (exports) ->

    class BaseAdapter

      connect: (database, doneFn, failFn) ->

      create: ->

      retrieve: ->

      update: ->

      destroy: ->
